extends Node2D

# The point of the arbiter is to evaluate contracts,
# its given a contract and tries to determine the utility/fairness for either side.

# The arbiter also records and routes signals to other contracts, so any contract must be routed to the arbiter to be enforced

#func valuate(contract):
#	return 500
#	return -200

#func handle_trigger(trigger):
#	if matching_contract(trigger):
#		trigget(contract)

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
